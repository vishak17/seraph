"""C12 — Dashboard (FR39-FR42).

Streamlit. Contains no analytics — every number is fetched, never computed. A
chart needing a value no component produces is a missing FR, not a dashboard
feature. RBAC is enforced at the gateway/transport layer, not the view (CT-8).

Compliance: Kite-derived data may not be publicly redistributed — keep this
local/demo-only (DATA-ACQUISITION §1).

Phase 9 (L8) — not implemented yet.
"""
