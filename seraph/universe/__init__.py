"""C3 — Universe Constructor (FR52, FR54).

Owns `universe_member_flag`, the rebalance calendar, the FR52 eligibility rule
(filter on >=1000 listing days *before* ranking by 6-month median TOTTRDVAL),
and the D3 buffer rule.

Phase 3 (L2) — not implemented yet.
"""
